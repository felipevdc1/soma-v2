# SONAR Report — 20260511-121746

**Projeto**: `/Users/felipevdc1/Documents/- projetos claude code/soma-v2`
**Spec**: `core/specs/015-soma-install/spec.md` (244 linhas, 19 ACs + AC-01b)
**HEAD**: `d92137a` (Wave 7 partial — install-e2e composite + skill-activation smoke doc)
**Pre-Gate**: #2 (release v2.2.0 approval) — this audit IS the gate
**Agents**: 5 read-only Explore (Architecture/Opus, Modules/Sonnet, Tests/Haiku, Config/Haiku, SpecAdherence/orchestrator)

> **Note on Agent 5**: First 4 agents dispatched as parallel Explore; thermal-guard.cjs misclassified them as compile-test (hook reads "test"/"build" keywords in prompt). Bypass marker consumed once; Agent 5 retried 3x blocked. Orchestrator (Opus) performed Agent 5 spec-adherence review manually — same read-only methodology, no edits.

---

## Sumário Executivo

| Território | CRITICAL | HIGH | MEDIUM | LOW | Total |
|------------|----------|------|--------|-----|-------|
| Architecture (Opus) | 0 | 7 | 4 | 2 | 13 |
| Modules (Sonnet) | 0 | 4 | 5 | 3 | 12 |
| Tests (Haiku) | 0 | 0 | 3 | 2 | 5 |
| Config (Haiku) | 0 | 2 | 1 | 0 | 3 |
| Spec Adherence (orchestrator) | 0 | 1 | 2 | 1 | 4 |
| **TOTAL** | **0** | **14** | **15** | **8** | **37** |

> Severidade reassessed pelo orchestrator. Agent 4 flagged 2 CRITICAL (manifest.json version skew + install.cjs LOC budget) — both downgraded:
> - **manifest.json version "2.1.0" vs package.json "2.2.0"**: investigação rasa indica que `core/manifest.json` campo `version` representa baseline schema version (não product release), separado de package.json. Felipe deve confirmar. Downgrade pra HIGH (operator confusion possível).
> - **install.cjs 1182 LOC vs ~200 LOC budget (plan.md §22)**: arquitetural drift real mas código funcional, tests GREEN, frozen libs PRISTINE. Não bloqueia release. HIGH (debt) mas não CRITICAL (ship).

**Decisão de fluxo**:
- 0 CRITICAL ✓
- 5 spec_violations encontradas (AC-15 force-resync, plan §22 LOC, plan §40-41 manifest baseline, AC-06 explicit test, AC-13 sub-effect core/INSTALL.md path)
- Per SONAR skill rules: ≥1 spec_violation → **❌ Step 9 FIX LOOP recomendado**
- Felipe override possível: ship v2.2.0 com violations documentadas + roadmap v2.3 fix?

---

## Findings por Severidade

### 🔴 CRITICAL

(none after orchestrator reassessment)

---

### 🟠 HIGH (14)

**[architecture]** `core/scripts/install.cjs:474-475, 805-814, 907-914`
> `--force-resync` flag parsed but never propagated to sync.cjs spawn args. Contract lie — AC-15 cross-harness parity advertises 6 flags including --force-resync that has no effect. Operators depending on it silently get BF-06 default abort.
> Fix: implement end-to-end OR remove from contract + skill + error msg hints
> **Spec violation: AC-15**

**[architecture]** `core/scripts/install.cjs:1107-1130`
> install.cjs requires frozen lib `lib/anchored-blocks.cjs` directly for inline sha-drift detection, duplicating BF-06 logic in sync.cjs:656-672. Violates plan.md D-01 (no require() of internals) + Article VIII (Anti-Abstraction). Drift logic now in TWO places.
> Fix: delete inline scan, always run full pipeline + let sync.cjs detect drift canonically OR factor to sync.cjs --check-only flag

**[architecture]** `core/scripts/install.cjs (1182 LOC) vs plan.md:22`
> install.cjs is 5.9x over the stated 200 LOC simplicity budget. Plan-1 Simplicity Gate breach. Concrete reductions identified: extract `handleSyncFailure()` helper (-50 LOC), move CLAUDE.md classification to lib/install-claude-md.cjs (-80 LOC), drop inline drift gate (-50 LOC) = net -180 LOC.
> Fix: extract helpers OR update plan to reflect reality + document Gate #1 approval of growth
> **Spec violation: plan.md §22 Phase -1 Simplicity Gate**

**[architecture]** `core/scripts/install.cjs:682,690`
> Step 0 CLAUDE.md classification hardcoded — ignores `--tool=codex`. Codex-only install with free-text AGENTS.md sails past Step 0 protection (AC-09 abort) and only fails downstream.
> Fix: skip Step 0 when flags.tool === 'codex' (cleanest) OR generalize to AGENTS.md detection

**[architecture]** `core/scripts/install.cjs:890-897 + project-bootloader.md`
> Template var `{{soma_home}}` rendered with SOURCE_CORE (`<repo>/core`) instead of installed location `~/.soma-v2/`. Bootloader block in `<project>/CLAUDE.md` points readers to non-existent paths in lab dir.
> Fix: set templateVars.soma_home = `path.join(os.homedir(), '.soma-v2')`

**[modules]** `core/scripts/install.cjs:140-141`
> snapshotClaudeMd() resolves SOMA_HOME via `path.resolve(SOMA_HOME, '..')` but sync.cjs uses SOMA_HOME directly. If SOMA_HOME=/Users/x/.soma-v2, snapshotClaudeMd writes to /Users/x/.snapshots/ (parent dir!) instead of /Users/x/.soma-v2/.snapshots/.
> Fix: drop the `..` traversal
> **Spec violation: AC-08 (snapshot path)**

**[modules]** `core/scripts/__tests__/sync-bf06-abort.test.cjs:39`
> Test targets installed `~/.soma-v2/scripts/sync.cjs` not in-repo `core/scripts/sync.cjs`. CI/fresh checkout without prior install uses stale sync — false positives/negatives possible.
> Fix: change SYNC_CJS to repo-local path `path.resolve(__dirname, '..', 'sync.cjs')`
> **Spec violation: Constitution Article III (Integration-First)**

**[modules]** `core/scripts/install.cjs:461,474`
> Same as architecture HIGH above — `flags.forceResync` parsed but never read. Dead code OR contract lie.

**[modules]** `core/scripts/install.cjs (1182 LOC)`
> Same as architecture HIGH — LOC budget breach.

**[modules]** `core/scripts/install.cjs:835-843 (Step 3) vs 935-942 (Step 3b)`
> Drift detection logic copy-pasted ~95% verbatim. DRY violation. Future change to detection silently diverges.
> Fix: extract `classifySyncFailure()` helper

**[config]** `core/manifest.json:3`
> Version "2.1.0" while package.json + AGENTS.md soma-install block say "2.2.0". Possibly schema vs product version distinction — needs Felipe confirmation.
> Fix: clarify field semantics; if product version, bump to "2.2.0"; if schema version, document distinction

**[config]** `core/scripts/install.cjs` 1182 LOC
> Same — escalated to CRITICAL by Agent 4, downgraded to HIGH by orchestrator (architectural debt, not runtime risk).

**[spec_adherence]** `core/scripts/__tests__/install.test.cjs` (no explicit T-NN for AC-06)
> AC-06 (path com espaço + hyphen leading — `/tmp/- soma test fresh hyphen`) does not have a dedicated test labeled `// @spec AC-06` or `T-07-S1`. Coverage may be implicit via test tmpdir paths but no explicit assertion.
> Fix: add T-07-S1 with path `/tmp/- soma test fresh hyphen` assertion
> **Spec violation: AC-06**

**[architecture]** `core/adapters/claude/commands/soma-install.md:45 vs core/INSTALL.md:10`
> Cross-harness Node version drift: Claude skill says ≥18, INSTALL.md mandates ≥22. Operators on Node 18-21 pass skill prereq + fail backbone CLI.
> Fix: update skill to ≥22 + add same prereq to Codex AGENTS.md block

---

### 🟡 MEDIUM (15)

**[architecture]** `core/adapters/codex/install-targets.json:6,23 + 18,29` — duplicate block_ids with different target_paths
**[architecture]** `package.json:3 + VERSION:1 + core/scripts/soma.cjs:31` — 3-way version drift (2.2.0 / 2.1.0 / soma-v2.1)
**[architecture]** `core/scripts/install.cjs:815-867 vs 920-963` — Step 3 vs Step 3b duplicated error handlers
**[architecture]** `core/scripts/install.cjs:901-905` — Step 3b silent continue if project adapter missing; AC-01b guarantee waived
**[architecture]** Missing `core/docs/INSTALL.md` per T-19 — exists at `core/INSTALL.md` + `docs/INSTALL.md` instead. Cross-link mismatch in skill ref.
**[modules]** `core/scripts/install.cjs:688` — `stateFilePathForStep0` dead variable
**[modules]** `core/scripts/install.cjs:882` — inner `const crypto = require('node:crypto')` shadows top-level
**[modules]** `core/scripts/install.cjs:562` — runDryRun emits `.soma/installed-state.json` (typo: should be `install-state.json`). AC-16 violation.
**[modules]** `core/scripts/install.cjs:820-868, 920-965` — drift detection duplication
**[modules]** `core/scripts/install.cjs` LOC budget (re-listed at MEDIUM by Agent 2)
**[modules]** Test helper `runInstall()` duplicated across 3 test files
**[tests]** `install.test.cjs T-08bis-S1` — missing assert for "This project uses SOMA v" preamble substring (AC-01b partial)
**[tests]** `sync-bf06-abort.test.cjs S4` — 5-element error msg order/completeness check incomplete (AC-19 partial)
**[tests]** `install.test.cjs T-08bis-S1` — `{{` regex check covers only 4 placeholders, not comprehensive `assert.strictEqual(content.match(/\{\{/g), null)` style
**[config]** `core/adapters/claude/commands/soma-install.md:42-46` — SOMA_HOME env var undocumented in skill Prereqs
**[spec_adherence]** AC-10 + AC-11 only have manual smoke procedure (no automated regression coverage). Documented intent (spec.md table marks "manual + future hook"), but accept regression risk.

---

### 🟢 LOW (8)

**[architecture]** install.cjs reaches into frozen lib `anchored-blocks.cjs` API (read-only) — encapsulation rationale weakened
**[architecture]** install-targets.project.json files have no schema parity smoke test
**[modules]** `core/scripts/install.cjs:1014-1016` — `orchestrateWithLock()` zero-value wrapper, inline at single call site
**[modules]** `core/scripts/install.cjs:563` — runDryRun emits `block.claude.CLAUDE.md.*` (dots) instead of canonical `CLAUDE_md.*` (underscores)
**[modules]** `mergeClaudioMd`/`replaceClaudioMd` typo (Claudio not Claude) — internal property only, contract is correct
**[modules]** `core/scripts/install.cjs:590-593` — parseSyncBlockId() single-use wrapper, inline at call site
**[tests]** `AC-05` test simulation could use chmod/EACCES more strictly (currently passes via SOMA_HOME=/nonexistent — cleaner but slightly different failure mode)
**[tests]** `AC-17` test could add reference to baseline commit `f3c2f0b` for traceability
**[spec_adherence]** Traceability table references `install.test.js` (not `.cjs`) for AC-01/AC-06/AC-16 — stale filename, real file is `.cjs`

---

## Spec Violations

| AC / Plan ref | Severidade | O que falta | Sugestão |
|---|---|---|---|
| AC-15 (cross-harness parity) | HIGH | `--force-resync` advertised but dead | Implement OR remove from contract |
| plan §22 (Simplicity Gate) | HIGH | install.cjs 1182 vs 200 LOC budget | Extract helpers (~-180 LOC) |
| AC-08 (snapshot path) | HIGH | SOMA_HOME `..` traversal writes wrong dir | Drop `..` |
| Constitution Article III | HIGH | sync-bf06 test targets installed not source | Switch to repo-local path |
| AC-06 (path edge) | HIGH | No explicit T-07-S1 for `/tmp/- soma test fresh hyphen` | Add dedicated test |
| AC-16 (state file) | MEDIUM | runDryRun emits wrong filename `installed-state.json` | Fix to `install-state.json` |
| AC-01b (bootloader content) | MEDIUM | Missing "This project uses SOMA v" substring check + `{{` regex incomplete | Strengthen test assertions |
| AC-19 (BF-06 5-elem msg) | MEDIUM | Order/completeness check incomplete | Add explicit grep for 5 elements |

---

## Notas de Auditoria

- 4 agentes Explore (read-only) + 1 orchestrator review (also read-only). Nenhum file editado.
- Thermal-guard hook bug capturado: classifica Explore agents como compile-test por keyword matching. Felipe memory `feedback_thermal_guard.md` cobre essa trap. v2.2-fix candidate: improve hook classification or whitelist Explore subagent_type.
- Frozen libs invariant: PRISTINE confirmed pre-audit (SHA256 match baselines).
- Test surface health: 56+ tests GREEN no v2.2 deliverables; 4 pre-existing fails (CC-07, SC-10a, SC-10b, AC-13 stale exit-1 expectation) — todos non-blocking.

---

## Decisão de Fluxo

### Opção A (rigorosa per SONAR skill): Step 9 FIX LOOP

5 spec_violations + 14 HIGH findings → fix-loop antes do release. ~4-8h work.

**Fix priorities (top 5)**:
1. AC-15 --force-resync wire-up (or contract removal)
2. AC-08 snapshotClaudeMd SOMA_HOME path fix (one-liner)
3. sync-bf06-abort test path correction (one-liner, Constitution gate)
4. Step 0 codex/AGENTS.md generalization (or explicit skip)
5. Template var `{{soma_home}}` rendered path fix (one-liner)

Items 2-5 are small fixes (~30min total). Item 1 is the substantive decision (implement or remove the flag).

### Opção B (Felipe override): Ship v2.2.0 com violations documented

Acceitar findings como known-issues, ship v2.2.0, fold fixes em v2.2.1 patch release dentro de 1-2 dias.

**Justificativa possível**:
- Zero CRITICAL (zero runtime risk)
- Frozen libs PRISTINE (no Constitution violation)
- 56+ tests GREEN (functional integrity verified)
- Empirical install pipeline works end-to-end (install-e2e.test.cjs 5/5)
- Violations são architectural debt + contract polish — não bloqueia user value

### Recomendação orchestrator

**Opção A "rigorosa-curta"**: cure os top 5 fixes (~30-60min Sonnet dispatch) ANTES de Gate #2. Bumpa v2.2.1 later pra MEDIUM/LOW items se valer. Best-of-both: ship quality + ship fast.

---

## Para iniciar FIX LOOP (Step 9)

Top 5 dispatches recomendados:

```bash
# Fix-01 — Wire --force-resync end-to-end OR contract removal (Felipe decide)
echo "fix(015): --force-resync wire-up [AC-15]"

# Fix-02 — snapshotClaudeMd SOMA_HOME path traversal
echo "fix(015): snapshotClaudeMd drop wrong parent traversal [AC-08]"

# Fix-03 — sync-bf06-abort.test.cjs path correction
echo "fix(015): sync-bf06 test target repo-local sync.cjs [Constitution Article III]"

# Fix-04 — Step 0 codex/AGENTS.md decision
echo "fix(015): Step 0 skip-on-codex OR generalize-to-AGENTS"

# Fix-05 — Template var soma_home rendered path
echo "fix(015): template var soma_home → installed location [AC-01b semantic]"
```

Estimated: ~60min total Sonnet dispatch + audit time.
