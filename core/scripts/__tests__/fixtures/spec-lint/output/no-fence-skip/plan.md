# Plan: fixture sem cerca de superfície

Este `plan.md` de propósito **não** tem o bloco cercado `soma-cli-surface`.
Só prosa comum — inclusive mencionando o verbo `spec-lint` de passagem, sem
invocá-lo, pra não acionar o check por acidente caso ele deixe de ser stub.
