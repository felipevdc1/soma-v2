# Spec: fixture sem cerca de superfície

**Feature ID:** fixture-no-fence

Fixture usado por `contract-lint-output.test.cjs` (T-03) para provar o
AC-06: sem o bloco cercado `soma-cli-surface` no `plan.md`, o check
`cli-surface` é pulado, e o rodapé precisa mostrá-lo em `pulados`, não em
`executados` nem silenciosamente ausente.
