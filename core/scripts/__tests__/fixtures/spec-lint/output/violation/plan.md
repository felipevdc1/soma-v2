# Plan: fixture com violação de superfície

Superfície de CLI fixada abaixo (mesma forma de `plan.md` §"Superfície de
CLI" da própria 017 — um positional, zero flags):

```soma-cli-surface
soma spec-lint <spec-dir>
```
