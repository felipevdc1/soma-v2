# Spec: fixture limpo

**Feature ID:** fixture-clean

Fixture usado por `contract-lint-output.test.cjs` (T-03) para provar que uma
spec sem violação produz exit 0, zero linhas de achado, e o rodapé mesmo
assim presente (CONTRACT-LINT-OUTPUT-01, AC-01).

Não tem `plan.md`, então nada aciona `cli-surface`. Não tem `tasks.md`,
então `parallel-collision` não tem o que colidir.
