# Quickstart

Rode o comando abaixo — uma invocação só, continuada por barra invertida em mais de duas linhas.

```bash
soma run dispatch-record end --run run-lab-001 \ 
  --task T-03 \
  --output-file /tmp/lab-output.md --metadata-file /tmp/lab-meta.json
```
