# Quickstart

Rode o comando abaixo — uma invocação só, continuada por barra invertida, mas faltando uma flag obrigatória mesmo depois de juntar.

```bash
soma run dispatch-record end --run run-lab-001 --task T-03 \
  --output-file /tmp/lab-output.md
```
