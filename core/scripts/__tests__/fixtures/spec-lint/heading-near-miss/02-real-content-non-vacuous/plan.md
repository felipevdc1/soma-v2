# Plan: fixture — heading-near-miss known-BOM

## Documentação de forma near-miss (como a spec 021 vai conter)

O bloco abaixo cita, como exemplo/documentação, uma forma near-miss — exatamente o tipo
de conteúdo que a spec 021 vai carregar para explicar a divergência entre as duas formas
canônicas. Se o check varresse linhas dentro de cerca, ele se acusaria a si mesmo:

```
### AC-05 — forma near-miss citada como exemplo, nunca como declaração real
```

Este bloco não deve ser acusado pelo check — a linha acima está dentro de uma cerca
` ``` `, não é um heading do documento.
