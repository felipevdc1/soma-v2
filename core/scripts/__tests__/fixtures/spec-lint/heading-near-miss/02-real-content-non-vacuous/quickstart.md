# Quickstart

Convenção real de walkthrough (medida em specs reais, ex. `003/quickstart.md:25`): headings
de seção citam o AC que o passo cobre, sem seguir a forma canônica de declaração — e isso
é legítimo, não um near-miss de verdade. `quickstart.md` fica fora do escopo do check por
causa exatamente disto.

## AC-01 — H2 detects src/ subdirs as modules

Passo 1: rode o comando abaixo e confira a saída.

```bash
soma spec-lint core/specs/019-gates-que-nao-pegam
```

## AC-02 + AC-03: dois ACs cobertos no mesmo passo

Passo 2: confira que os dois ACs foram exercitados juntos.
