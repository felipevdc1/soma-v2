# Contract: fixture — heading-near-miss known-RUIM

Fixture de teste para o check `heading-near-miss` (AC-01, spec 019). Corpus item 1 —
conhecido-RUIM, terceiro arquivo em escopo (prova que `contracts/*.md` também é varrido).

### AC-9 sem dois-pontos

Texto de exemplo — este heading tem exatos 3 `#` (a contagem canônica), mas falta o
dois-pontos depois do número. Não casa `^### AC-\d+:`.
