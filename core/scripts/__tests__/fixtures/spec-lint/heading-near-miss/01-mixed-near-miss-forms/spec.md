# Spec: fixture — heading-near-miss known-RUIM

Fixture de teste para o check `heading-near-miss` (AC-01, spec 019). Corpus item 1 —
conhecido-RUIM. Duas formas diferentes de near-miss, no mesmo arquivo.

## AC-02 + AC-03: dois ACs declarados no mesmo heading

Texto de exemplo — este heading tem 2 `#` e cita dois ACs, não casa a forma canônica
`### AC-01:` de nenhum dos dois jeitos.

#### AC-7 sem dois pontos

Texto de exemplo — este heading tem 4 `#` e não termina em dois-pontos.
