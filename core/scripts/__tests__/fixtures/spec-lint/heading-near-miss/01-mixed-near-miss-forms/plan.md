# Plan: fixture — heading-near-miss known-RUIM

Fixture de teste para o check `heading-near-miss` (AC-01, spec 019). Corpus item 1 —
conhecido-RUIM, segundo arquivo em escopo.

## AC-01 — descrição não numerada com travessão

Texto de exemplo — este heading tem 2 `#` e um travessão em vez de dois-pontos, não casa
a forma canônica.
