---
schema: soma-context/v1
project: {{PROJECT_NAME}}
last_updated: {{ISO8601_DATE}}
---

# Module Context Routing

Maps task keywords to module slugs for auto-load injection into agent dispatches.
Add rows as you create modules — keyword matching is case-insensitive substring.

| Keyword       | Module Slug   |
|---------------|---------------|
| # example:    | # example:    |
| # auth        | # auth-system |
| # billing     | # billing     |
