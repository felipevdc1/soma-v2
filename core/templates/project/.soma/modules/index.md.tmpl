# {{PROJECT_NAME}} — Module Index

<!-- TEMPLATE: soma-v2.1 modules/index.md stub. Fill during `soma init --existing` or `soma module add`. -->
<!-- expansion_owner: soma init / soma module add (Phase 3-4) -->

---
schema: soma-modules-index/v1
project: "{{PROJECT_NAME}}"
last_verified: "{{ISO8601_DATE}}"
---

## Modules

| Module | Doc | Status | Last Verified |
|---|---|---|---|
| (fill after `soma init --existing` proposes modules from real folders/domains) | | hypothesis | — |

## Notes
- `hypothesis` = inferred by agent, not yet verified against source/tests or human confirmation.
- `active` = verified (requires human review per Decision D-C9).
- Do not over-document before real work exists.
