# {{PROJECT_NAME}} — Agent Bootloader

<!-- TEMPLATE: soma-v2.1 project bootloader stub. Fill during `soma init`. -->
<!-- expansion_owner: soma init (Phase 3) -->

## Project Context

<!-- soma-v2:start id=project.AGENTS.bootloader version=2.1.0-draft sha256=FILL_AT_INSTALL -->
Detect whether this project has `.soma/`. If yes, read `.soma/CONTEXT.md` and the relevant module docs.
Use compressed SOMA loop for small tasks; escalate to 10-step protocol for medium/large/risky work.
Never claim done without evidence.
<!-- soma-v2:end id=project.AGENTS.bootloader -->

## Routing

- Project context: `.soma/CONTEXT.md`
- Module index: `.soma/modules/index.md`
- SOMA_HOME docs: `~/.soma-v2/docs/`

## Non-responsibilities

- Do not embed the full 10-step protocol inline.
- Do not duplicate all SOMA docs.
- Do not carry project-specific knowledge globally.
